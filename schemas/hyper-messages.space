space messages
key uuid
attributes
  string user,
  string to,
  string from,
  string subject,
  string cc,
  string bcc,
  string date,
  string folder,
  timestamp(second) intdate,
  timestamp(second) stored,
  timestamp(second) last_modified,
  int size,
  set(string) flags,
  document structure,
  int imap_uid,
  string pop3_uidl,
  document annotations
subspace user
index intdate
index folder
index flags
index last_modified
index imap_uid
create 1 partitions
tolerate 1 failures

