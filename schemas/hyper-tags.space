space tags
key user
attributes
  set(string) tags,
  int created,
  int modified,
  int size,
  map(string, int) tag_count,
  map(string, int) tag_unread_count,
  map(string, int) tag_created,
  map(string, int) tag_modified
create 8 partitions
tolerate 1 failures

